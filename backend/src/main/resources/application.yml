micronaut:
  application:
    name: who-gets-what
  io:
    watch:
      paths: src/main
      restart: true
  router:
    static-resources:
      default:
        enabled: true
        mapping: /**
        paths:
          - classpath:web
